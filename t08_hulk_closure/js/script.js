//object literal syntax
let concat0 = {
   count:0
  }

  function concat(firstPhrase, secondPhrase) {
    this.firstName = first
    this.lastName = last
    
  }

  var person2 = new Person('alex', 'ritzcovan');
  alert (person2.firstName);